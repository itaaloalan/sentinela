# Core
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic-settings>=2.6
PyJWT>=2.9
httpx>=0.27
python-multipart>=0.0.12
sqlmodel>=0.0.22          # persistência (Fase 2): câmeras + usuários
pwdlib[bcrypt]>=0.2.1     # hash de senha (Fase 2)
onvif-zeep>=0.2.12        # PTZ via ONVIF (import lazy em app/ptz.py)
pillow>=11.0              # crop do frame na inferência de teste (app/inference.py)

# --- dev / testes (gate de 100% de cobertura) ---
pytest>=8.3
pytest-asyncio>=0.24
pytest-cov>=6.0
respx>=0.21               # mock das chamadas httpx (go2rtc / ntfy)

# --- extras (descomentar quando implementar as fases) ---
# onvif-zeep>=0.2.12        # PTZ via ONVIF (Fase 3)
# paho-mqtt>=2.1            # se optar por eventos via MQTT
# pillow>=11.0              # manipular/cropar snapshots
#
# IA (Fase 4) — pesados; instale no serviço ai/ separado:
# ultralytics>=8.3          # YOLO classificação (AGPL-3.0)
# opencv-python-headless
